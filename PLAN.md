# 세븐나이츠 리버스 메타봇 — 개발 기획안 (Claude Code 작업 지시서)

## 0. 한 줄 요약
카카오톡 챗봇으로 세븐나이츠 리버스 인게임 메타(공성전·PVP·레이드·원소던전의
조합/세팅/카운터)를 안내한다. **정형 조회(시나리오) + AI 자유대화** 하이브리드.

---

## 1. 확정 기술 스택 (재검토·변경 금지, 이유 포함)

| 영역 | 선택 | 이유 |
|---|---|---|
| 챗봇 플랫폼 | 카카오 i 오픈빌더 | 카카오톡 공식 스킬(웹훅) 구조 |
| 스킬 서버 | **Cloudflare Workers** | 무카드·무잠듦. Render 무료는 15분 잠듦→콜드스타트 30~60초로 5초 제한 위반. Firebase Functions는 Blaze(카드) 필요 |
| DB | **Firebase Firestore** (Spark 무료) | 무카드. 지식 베이스 저장소 |
| 검수 웹 | **Firebase Hosting** (무료) | 무카드. 추출 데이터 사람 검수 UI |
| AI | Gemini API | 자유대화 답변 생성 + Vision 데이터 추출 |
| 자동 캡처(후순위) | GitHub Actions cron + Playwright | 무료 크론. 지정 공략글 스크린샷 수집 |

> **무카드 제약이 최상위 원칙이다.** Firebase Functions·Cloud Run 등 과금·카드 필요 플랫폼은 사용 금지.

---

## 2. 핵심 제약 (반드시 준수)

1. **카카오 5초 응답 제한** — 정형 조회는 동기 응답으로 처리(빠름). 느린 Gemini 답변만 콜백(`useCallback` + `callbackUrl`, Worker `ctx.waitUntil`)으로 비동기 처리.
2. **콜백은 관문이 있다** — 콜백 API는 챗봇을 "AI 챗봇"으로 전환해야 쓸 수 있고, **봇 테스트 시뮬레이터에서 테스트 불가**(실채널에서만 확인). 전환 방식(이메일 신청/심사)은 자료마다 엇갈리므로 착수 전 관리자센터 공지에서 현재 방식 확인.
3. **Vision 추출은 무조건 사람 검수 후 공개** — 무인 자동 게시 금지. 오답이 그대로 "조언"으로 나가면 봇 신뢰가 무너진다. 자동/수동 모두 승인 큐를 거친다.
4. **메타는 패치마다 바뀐다** — 데이터 갱신을 전제로 설계(공개본은 언제든 교체 가능).
5. **저작권/ToS** — 커뮤니티 공략은 타인 저작물. 내부용 우선, 외부 공개 시 출처/허락 고려. 자동 스크래핑은 반자동(후보만 수집→사람 승인)으로만.
6. **시크릿 하드코딩 금지** — Gemini 키·서비스계정 등은 `wrangler secret`/환경변수. 코드·리포에 절대 커밋 금지.

---

## 3. 데이터 스키마 (Firestore)

컬렉션 2개:
- `검수대기/{id}` — Vision 추출 후보 (원본 이미지 URL + 추출 JSON + `상태`). 봇은 조회하지 않음.
- `공개/{id}` — 승인된 공략. 봇이 조회하는 유일한 소스.

통합 문서 스키마 (모든 콘텐츠타입 공용):
```json
{
  "콘텐츠타입": "공성전",              // 공성전 | PVP | 레이드 | 원소던전
  "세부": "루디 (나타/리나)",          // 상대/스테이지/조합 구분
  "키워드": ["공성전루디", "나타리나"], // 매칭용(공백 제거 부분일치)
  "약점속성": null,                    // 원소던전/PVP에서 사용
  "영웅": [
    { "이름": "리나", "진형": "앞줄", "장비": "성기사 생명력%",
      "부옵": "생명력 최대한", "추천펫": "윈디", "코멘트": null }
  ],
  "스킬순서": ["1라", "2라(4번)", "8턴", "12턴"],
  "카운터": [],                        // PVP 상성용
  "메모": "48턴에 도트 걸려있어야 쿨돔",
  "출처": "DCinside/네이버라운지 URL",
  "갱신일": "2026-09-18"
}
```

Firestore 보안 규칙(`firestore.rules`):
- `공개`: read 허용(공개 게임 정보), write 거부(검수 페이지/admin만 기록)
- `검수대기`: 인증된 사용자(운영자)만 read/write

---

## 4. 리포 구조
```
/worker/          # Cloudflare Worker (스킬 서버)  ← worker.js, wrangler.toml 기존 뼈대 있음
/review-web/      # 검수 페이지 (Firebase Hosting)
/pipeline/        # Gemini Vision 추출 스크립트
/feeder/          # (Phase 4) 자동 캡처
firestore.rules
```

---

## 5. 개발 단계 (Goal-Driven — 각 단계 성공기준 통과 후에만 다음으로)

### Phase 1 — 핵심 봇 (정형 조회, 동기)
**M1a** *(기존 뼈대 존재: worker.js)* — Worker가 오픈빌더 스킬 규격대로 동기 응답(스텁 데이터).
- 성공기준: 시뮬레이터에서 `공성전 루디` → 세팅 응답 / 임의 입력 → 미매칭 안내 / 응답 5초 내.

**M1b** — `findComp()`를 Firestore `공개` 컬렉션 조회로 교체(읽기, 동기).
- 구현 방향: Worker에서 Firestore REST 읽기. **시크릿 없이 되도록 `공개` 컬렉션 public-read 규칙을 우선** 시도하고, 안 되면 서비스계정 JWT(Web Crypto RS256 서명) 폴백. 나머지 코드는 건드리지 않는다(외과적 변경).
- 성공기준: `공개`에 넣은 실제 조합이 시뮬레이터에서 조회된다.

### Phase 2 — 데이터 파이프라인 (수동 우선)
- **Vision 추출 스크립트**(`/pipeline`): 공략 이미지 → §3 스키마 JSON. 확신 낮은 필드는 표시.
- **검수 페이지**(`/review-web`, Firebase Hosting): 원본 이미지 + 추출 JSON 나란히 표시, 수정/승인 → `공개`로 이동. Firebase Auth로 운영자만 접근.
- 성공기준: 공략 스크린샷 1장 → JSON 후보 생성 → 검수 페이지에서 승인 → Phase 1 봇이 그 데이터를 응답한다.

### Phase 3 — AI 자유대화 (콜백)
- AI 챗봇 전환(방식 확인 후).
- 자유발화 라우팅: 폴백 블록 → `공개` 검색 + Gemini 요약/추천(RAG). **DB에 없는 정보를 지어내지 말 것**(근거 없는 답변 금지).
- 콜백: `useCallback`로 즉시 ack("잠시만요") → `ctx.waitUntil`로 Gemini 처리 → `callbackUrl`에 최종 답변 POST.
- 성공기준: 실채널에서 자유 질문 → 5초를 넘겨도 답이 도착하고, 답 내용이 DB 근거와 일치한다.

### Phase 4 — 자동 캡처 피더 (후순위)
- GitHub Actions cron + Playwright: 지정 URL 목록 렌더링 → 공략 이미지 다운로드 → Vision → `검수대기`에 후보 투입(사람 승인 전까지 비공개).
- 성공기준: 크론 실행 시 신규 후보가 `검수대기`에 쌓이고, 승인 전에는 봇 응답에 나오지 않는다.

---

## 6. 작업 원칙
- 전역 CLAUDE.md 원칙(단순 우선, 외과적 변경, 단계별 검증) 준수.
- **한 번에 한 단계만.** 여러 Phase 동시 진행 금지. 각 단계는 성공기준을 실제로 통과시킨 뒤 다음으로.
- 코드는 과설계 금지 — 요구된 것만. 스텁→실구현 교체 시 인접 코드 개선/리팩터 금지.
- §2 제약(무카드·5초·검수 필수·시크릿 보호) 위반 시 중단하고 알릴 것.

---

## 7. 지금 시작할 작업
> Phase 1 **M1b**부터 시작한다. 기존 `worker/worker.js`의 `findComp()`를 Firestore `공개`
> 컬렉션 조회로 교체하고, `firestore.rules`로 public-read 규칙을 세팅한 뒤, §5 M1b 성공기준
> (`공개`에 넣은 실제 조합이 시뮬레이터에서 조회됨)을 통과시켜라. 그 전에 착수 확인용으로
> 변경 계획을 3줄로 먼저 제시할 것.
